# Point of Instability

โปรเจกต์นี้เป็น Web Application ที่ประกอบด้วย:
- **Client:** Angular (อยู่ในโฟลเดอร์ `client`)
- **Server:** Rust (อยู่ในโฟลเดอร์ `server`)

## วิธีการรัน
1. เข้าไปที่โฟลเดอร์ server แล้วรัน `cargo run`
2. เข้าไปที่โฟลเดอร์ client แล้วรัน `npm start`